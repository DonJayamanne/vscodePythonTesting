{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create Virtual Env",
            "type": "shell",
            "command": "virtualenv .venv && ./.venv/bin/python -m pip install -e .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
